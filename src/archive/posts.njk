---
layout: base.njk
title: L'Archive / Manuscripts
description: Curated manuscript records from Forgotten Industries.
permalink: /archive/posts/
eleventyExcludeFromCollections: true
---
<section class="masthead">
  <div class="section-inner">
    <p class="section-label">L'Archive / Manuscripts</p>
    <h1>Les Manuscrits</h1>
    <p class="tagline">
      Authored works, doctrines, essays, complete texts, and the feed-facing
      surface of the archive.
    </p>
  </div>
</section>

<section class="evidence-band">
  <div class="section-inner band-grid">
    <div>
      <p class="section-label">{{ collections.posts | countLabel("record", "records") }}</p>
      <h2>The feed is selective. The archive is complete.</h2>
    </div>

    <ul class="link-list archive-record-list">
      {% for post in collections.posts | reverse %}
      <li>
        <p class="entry-note">
          <time datetime="{{ post.date | isoDate }}">{{ post.date | readableDate }}</time>
          / manuscript{% if post.data.type %} / {{ post.data.type }}{% endif %}
        </p>
        <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
        {% if post.data.description %}
        <p>{{ post.data.description }}</p>
        {% endif %}
      </li>
      {% endfor %}
    </ul>
  </div>
</section>
