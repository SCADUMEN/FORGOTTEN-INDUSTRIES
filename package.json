{
  "name": "@forgotten-industries/archive",
  "version": "0.1.0",
  "description": "Canonical archive data for Forgotten Industries machines, parts, field logs, and restoration dossiers.",
  "type": "module",
  "sideEffects": false,
  "files": [
    "dist",
    "src"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.ts",
      "default": "./dist/index.ts"
    },
    "./data": "./dist/forgotten-industries.json"
  },
  "scripts": {
    "build": "ruby scripts/build.rb",
    "build:archive": "ruby scripts/build.rb",
    "build:css": "sh scripts/build_css.sh --minify",
    "watch:css": "sh scripts/build_css.sh --watch",
    "clean:site": "node scripts/clean_site_output.cjs",
    "build:continuance": "node scripts/build_continuance_index.cjs && (cd continuance && vite build)",
    "dev:continuance": "npm run build:css && node scripts/build_continuance_index.cjs && (cd continuance && vite)",
    "build:site": "npm run clean:site && ruby scripts/build.rb && npm run build:continuance && eleventy && sh scripts/build_css.sh --minify && npm run audit:public",
    "serve:site": "npm run clean:site && ruby scripts/build.rb && npm run build:continuance && eleventy && sh scripts/build_css.sh && eleventy --serve",
    "audit:metadata": "node scripts/audit_metadata_profile.cjs",
    "audit:public": "node scripts/audit_public_surface.cjs",
    "scrub:exif": "node scripts/scrub_exif.cjs",
    "sweep:metadata": "npm run build && node scripts/audit_metadata_profile.cjs --write",
    "pretty": "prettier --write .",
    "pretest": "npm run build:site",
    "test": "vitest run",
    "pretest:unit": "npm run build:site",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "keywords": [
    "archive",
    "restoration",
    "hardware",
    "field-notes",
    "forgotten-industries"
  ],
  "author": "Matthew Taylor Marx",
  "license": "UNLICENSED",
  "devDependencies": {
    "@11ty/eleventy": "3.1.6",
    "@11ty/eleventy-plugin-rss": "3.0.0",
    "@playwright/test": "1.61.0",
    "@tailwindcss/cli": "4.3.2",
    "@vitejs/plugin-react": "6.0.3",
    "exiftool-vendored": "37.0.0",
    "http-server": "14.1.1",
    "minisearch": "7.2.0",
    "prettier": "3.8.3",
    "prettier-plugin-tailwindcss": "0.8.0",
    "react": "19.2.7",
    "react-dom": "19.2.7",
    "tailwindcss": "4.3.2",
    "vite": "8.1.5",
    "vitest": "4.1.9"
  },
  "engines": {
    "node": ">=22",
    "npm": ">=10.0.0"
  }
}
