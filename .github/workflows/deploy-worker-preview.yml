name: Deploy Cloudflare Worker Preview

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cloudflare-worker-preview
  cancel-in-progress: false

jobs:
  deploy:
    name: Build and deploy Worker preview
    if: github.repository == 'SCADUMEN/FORGOTTEN-INDUSTRIES'
    runs-on: ubuntu-latest
    environment:
      name: cloudflare-worker-preview
      url: ${{ steps.deploy.outputs.deployment-url }}

    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version-file: .nvmrc

      - name: Setup Ruby
        uses: ruby/setup-ruby@8e41b362d2589a22a44c1cfa214b3c83052c195b # v1
        with:
          ruby-version: '3.3'

      - name: Install dependencies
        run: npm ci

      - name: Build Eleventy archive
        run: npm run build:site

      - name: Audit public release surface
        run: npm run audit:public

      - name: Validate Worker asset bundle
        run: npx wrangler deploy --dry-run

      - name: Deploy Worker preview
        id: deploy
        uses: cloudflare/wrangler-action@9acf94ace14e7dc412b076f2c5c20b8ce93c79cd # v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: '4.112.0'
          command: deploy

      - name: Verify Worker preview
        env:
          WORKER_BASE_URL: ${{ steps.deploy.outputs.deployment-url }}
        run: npm run verify:worker -- "$WORKER_BASE_URL"
