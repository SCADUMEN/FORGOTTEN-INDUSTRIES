# CLAUDE.md

Read `AGENTS.md` — it is the canonical entry point for all coding agents in this repository.

Tooling, build commands, and project structure are documented in `README.md`.

## Design

Reference the styleguide (`/styleguide/`, rendered from `src/styleguide.njk`) for all design decisions — type, color tokens, components, route color families, and the heuristics that hold them together. It is rendered with the site's own classes and tokens, so it reflects what actually ships. Keep it in sync when the design system changes.
